apiVersion: v1
kind: Service
metadata:
  name: hadal
spec:
  # Same pattern as nodejs-web — simplest thing that works without an
  # Ingress controller wired up yet. k3s reserves 30000-32767 for this.
  type: NodePort
  selector:
    app: hadal
  ports:
    - port: 80
      targetPort: 80
