# Phase 1 leaf — filters the build context before it ever reaches the daemon.
# See: docker system map, Phase 1 / Step 1.1

node_modules
dist

# Unrelated sibling projects that live in this repo root
somali_learning_flutter
story-hover-translator
Somali language learning app

# Stray root-level files, not part of the app
*.png
diagram.py
curriculum.txt
rawcurriculum.txt
unit1-lessons-summary.txt
assignments.json
rise-admin4

# VCS / tooling
.git
.claude
.DS_Store
*.log
